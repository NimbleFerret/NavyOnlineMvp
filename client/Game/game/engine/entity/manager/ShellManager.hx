package game.engine.entity.manager;

import game.engine.entity.manager.BaseEntityManager;

class ShellManager extends BaseEntityManager {
	public function new() {}
	// public function shootFromLeftShipSide(ship:EngineShipEntity) {
	// 	final r = ship.rotation + MathUtils.degreeToRads(-90);
	// var shell1 = new Shell(++shellsCount, playerShip.id, s2d, playerShip.leftCanon1.localToGlobal().x, playerShip.leftCanon1.localToGlobal().y, r);
	// shells.set(shellsCount, shell1);
	// var shell2 = new Shell(++shellsCount, playerShip.id, s2d, playerShip.leftCanon2.localToGlobal().x, playerShip.leftCanon2.localToGlobal().y, r);
	// shells.set(shellsCount, shell2);
	// var shell3 = new Shell(++shellsCount, playerShip.id, s2d, playerShip.leftCanon3.localToGlobal().x, playerShip.leftCanon3.localToGlobal().y, r);
	// shells.set(shellsCount, shell3);
	// final shell = new ShellEntity();
	// add();
	// }
}
