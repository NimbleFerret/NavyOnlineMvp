/* eslint-disable prettier/prettier */

import { v4 as uuidv4 } from 'uuid';
import { createCanvas, loadImage } from "canvas";
import { RandomService } from "src/random/random.service";
import { MoralisService } from 'src/moralis/moralis.service';
import { ShipStats } from 'src/cronos/cronos.service';

export class NftIslandGenerator {

    async generateFounderIsland(index: number, maxIndex: number, shipStats: ShipStats) {
    }

}